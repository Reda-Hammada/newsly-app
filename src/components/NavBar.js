import SearchBar from "./SearchBar";

function NavBar() {
  return (
    <div>
      <header>
        <nav>
          {/* Logo */}
          <div>Logo</div>
          {/* Logo */}

          {/* Search bar */}
          <div>
            <SearchBar />
          </div>
          {/* Search bar */}
        </nav>
      </header>
    </div>
  );
}

export default NavBar;
