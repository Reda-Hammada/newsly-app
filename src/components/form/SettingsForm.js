import React from "react";
import { useForm } from "react-hook-form";

const SettingsForm = () => {
    const {
        register,
        handleSubmit,
        formState: { errors },
      } = useForm();

      return(
            <div>
                
            </div>
      )
    
};

export default SettingsForm;
